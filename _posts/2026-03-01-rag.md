--- 

tags: [vector-database, graph-database, llm]  

---


Databases come in various forms for example, key-value (dictionary) store, document store, graph database, columnar store. Vector database is one form that saves information as multidimensional vectors, an organized store of vector embeddings of data. 

A vector database is one that indexes and stores vector embeddings for fast retrieval of data by similarity search or vector search. Vector databases like pinecone and weaviate are commercially available, while popular open-source ones are chroma and qdrant. Elastic search is one widely deployed vector database, and lanceDB is open-source but offers commercial products with additional features and managed services.

VdBs are designed for efficiency and scalability unlike traditional keyword-matching databases. 

Key differences between VdB and traditional database:





VdBs are optimized for vectorized data storage and retrieval while traditional databases are optimized for scalar values.



VdBs efficiently handle high-dimensional data.



VdBs have advanced indexing and query techniques for similarity search, traditional databases have simple indexing method only.



VdBs are designed for seamless integration with machine learning (ML), and traditional databases have limited compatibility with ML workflows. 


VdBs usually maintain two indices, a vector index and a metadata index. It performs the metadata filtering either before or after the vector search. 

The [embedding models](https://www.nvidia.com/en-us/glossary/vector-database/) utilized for vectorization of the input data based on their types (text, image, audio, video). are neural networks with transformer architecture. 

VdBs are immensely useful as semantic search engines and for [recommendation systems](https://www.linkedin.com/posts/drranja-sarkar_recommenders-amazon-knowledgegraphs-activity-7305128847052562433--Hvl) leveraging user-item similarity (collaborative filtering).

VdBs use distances (e.g. Euclidean, cosine similarity) in the embedding space to represent (semantic) similarity between query vector and vectors in the database for information retrieval. 

 In contrast to traditional keyword search, vector search offers a more flexible and optimized way of searching.

 The query/prompt (as an embedded vector) is used for semantic search through clustering algorithms to arrive at relevant output/result.

VdBs enhance the capability of an LLM by supporting with memory, that is helping the model remember what was said earlier to hold a proper conversation with the prompt and with factual information outside of the general knowhow. This is called retrieval-augmented generation (RAG). 

A RAG system typically has a retrieval component linked to a knowledge-base which is typically a pre-trained LLM, and a generative component. VdBs can effectively provide LLMs with state by updating the information, without which the (trained) model knowledge is frozen.

The retrieve-generate process can be broadly of 3 types:

i) iterative

ii) recursive

<img width="343" height="255" alt="11" src="https://github.com/user-attachments/assets/8123cbe3-a78e-4779-a983-836b5500153e" />

iii) adaptive

<img width="230" height="233" alt="22" src="https://github.com/user-attachments/assets/d1b37bf5-87c5-43fc-9422-3156c77f4a80" />


The adaptive method enhances the RAG pipeline, as it can be either model-based or rule-based. Adaptive RAG produces the best results owing to its flexible and active control of the process. How you retrieve the relevant information dictates how much better is your LLM application.  

<img width="205" height="186" alt="llm" src="https://github.com/user-attachments/assets/fb26cb42-f44c-4957-8ebf-67a812c91e1c" />


If one moves outside the general knowledge into domain-specific areas where the relevant facts may not have been a part of model training, LLMs start to “hallucinate”. Here’s where RAG by VdB comes in handy. 

💡How does one reduce LLM hallucinations? 

One uses a vector search engine to retrieve relevant information and pipe it to the LLM’s context window for factually accurate results. RAG gives LLMs access to new information via vector databases and graph databases. 

Given the probabilistic paradigm, one can only reduce hallucinations and not get rid of them completely.

Popular open-source frameworks to support the creation of LLM applications with RAG are LangChain, LlamaIndex. 

LangChain typically has four components: chain (workflow), index (mapping to data storage location), memory (saving capacity), and prompt (instruction/query). 

 An open-source framework to evaluate LLMs is [DeepEval](https://deepeval.com). A human-in-the-loop for validating LLM output wrt user expectations is crucial. 
---

In vector databases, vector similarity is used as discussed above however, data stored in graph databases is better suited to reduce hallucinations and answer complex queries with more relevance and accuracy, due to their ability to provide the complex relationships between data objects. This is why systems leveraging graphRAG are on the rise. But that’s another post!
